# 2026-03-12 Daily Memory

## 小红书发布技术问题（11:52-12:41）

### 任务背景
栋少询问猫妹的小红书推文内容，并要求再次尝试发布（可以不上传图片）。

### 推文内容
- **标题**：招财猫妹的AI剪辑副业秘籍
- **正文**：猫妹风格介绍（黄蓝异瞳微牛、AI剪辑副业、两大绝活）
- **图片**：3 张（已保存在 /root/.openclaw/media/inbound/）
- **标签**：AI剪辑、副业赚钱、小红书运营、剪映教程、自媒体、内容创作、效率工具、批量生产
- **草稿位置**：`/root/.openclaw/workspace-g/xiaohongshu-data/maomei-draft.md`

### 技术问题：mcporter 调用超时

#### 问题现象
- mcporter 调用 `xiaohongshu.publish_content` 持续超时
- 尝试 60s 超时：失败
- 尝试 180s 超时：失败
- 命令一直在运行，但最终超时退出

#### 已尝试的方法
1. ✅ 登录状态检查：`mcporter call xiaohongshu.check_login_status` 返回已登录
2. ✅ agent-reach doctor：显示小红书 MCP 完整可用（阅读、搜索、发帖、评论、点赞）
3. ❌ mcporter 命令行调用（key=value 语法）：超时
4. ❌ Python 直接调用 MCP HTTP API：会话初始化问题（`method "tools/call" is invalid during session initialization`）
5. ❌ 派工给啾啾：Gateway 超时（sessions_send 超时 62000ms）

#### 技术细节
- **mcporter 参数解析问题**：
  - `--param` 语法：参数过多错误
  - `--json` 语法：缺少必需属性错误
  - 位置参数：类型错误（数组被识别为字符串）
  - key=value 语法：可以解析，但调用超时
- **MCP HTTP API 问题**：
  - 需要先 `initialize` 再 `notifications/initialized`
  - 但调用 `tools/call` 时仍报错"invalid during session initialization"
- **小红书 MCP 服务**：
  - 端口：http://localhost:18060/mcp
  - 状态：doctor 显示完整可用
  - 可能原因：上传 3 张图片耗时过长，或服务响应慢

#### 可能原因分析
1. 小红书 MCP 服务响应慢（上传图片耗时）
2. MCP HTTP 服务需要重启
3. 网络问题或小红书平台限流
4. mcporter 与 MCP 服务之间的通信问题

### 待解决
- 重启小红书 MCP 服务
- 或排查 MCP 服务日志
- 或手动在小红书 App 创建草稿

## 协作问题观察

### Gateway 超时问题（持续）
- sessions_send 连续超时（62000ms）
- 啾啾 session 可能处理时间过长
- 建议：复杂任务优先在 G 的 session 内完成，避免跨 Agent 通信开销

## 技术经验

### mcporter 调用小红书 MCP
- ✅ 登录检查：`mcporter call xiaohongshu.check_login_status`
- ✅ 列出工具：`mcporter list xiaohongshu --all-parameters`
- ❌ 发布内容：`mcporter call xiaohongshu.publish_content` 超时
- 参数顺序：`content, images, schedule_at?, tags?, title`
- 超时配置：`MCPORTER_CALL_TIMEOUT=180000`（默认 60000ms）

### MCP HTTP API 直接调用
- 需要先初始化会话：`initialize` + `notifications/initialized`
- 但调用 `tools/call` 时仍有问题
- 可能需要使用 WebSocket 而非 HTTP

### agent-reach 小红书 MCP
- 安装：`pip install https://github.com/Panniantong/agent-reach/archive/main.zip`
- 配置：`agent-reach configure xiaohongshu-cookies "..."`
- 状态检查：`agent-reach doctor`
- Cookie 位置：`/root/.openclaw/workspace-g/xiaohongshu-data/cookies.json`
