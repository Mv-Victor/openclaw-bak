# 2026-02-27 Daily Memory

## Drama.Land 深度技术调研（延续 02-26）

### 任务
栋少要求深入调研 Drama.Land (cn.drama.land) 的完整实现，目标是后续复刻。

### 成果
完整技术调研报告已写入 `/root/.openclaw/workspace/drama-land-research.md`，覆盖：
1. 技术栈（前端+后端+基础设施）
2. 详细页面设计（7个页面，15张截图）
3. 后端接口设计（16+ API，含请求/响应结构）
4. 数据模型设计（User→Project→Series→Episode→Scene→Segment）
5. 工作流设计（三种模式的节点 DAG + AI 生成机制）

### 关键技术发现
- 前端：Next.js App Router + React Flow (@xyflow/react) + Zustand + Tailwind + Radix UI
- 认证：Firebase Auth (project: drama-land-studio)
- 存储：Google Cloud Storage (bucket: dramaland-public)
- 视频导出：Google Cloud Run
- 实时推送：SSE
- 音乐生成：疑似对接 Udio
- API 前缀：/backend-api/api/v1/
- 双区域部署：cn.drama.land（中国）/ drama.land（国际）

### 技术过程
- 服务器 3.6G 内存，agent-browser OOM，改用 Playwright + chromium 直接驱动
- Firebase Auth API 被墙（googleapis.com），通过页面 UI 登录绕过
- 页面有遮罩层弹窗，需 evaluate 移除后才能点击
- 登录按钮文字是"注册/登录"而非"登录"，需用 nth(1) 定位弹窗内的按钮
- 用 page.on('request'/'response') 抓 API 请求和响应数据
- 分多个脚本执行避免内存溢出

### 截图保存
/tmp/drama_01~15.png（首页、登录、项目列表、画布、节点面板、剧本详情、订阅、任务中心）

### 栋少提供的测试账号
- 账号: [email]
- 密码: [password]
- Firebase UID: FLP6Yg0BKZhO5fdy9WrzgaQKA8F2
- 用途: Drama.Land 技术调研

---

## DreamX Studio 前端搭建

### 任务
栋少要求参考 skills 完成整个项目前端编写，后端数据先 mock。

### 项目位置
`/root/dreamx-studio/`

### 技术栈
- Next.js 14 App Router
- React Flow (@xyflow/react) — 画布式创作
- Zustand + Immer — 状态管理
- Tailwind CSS — 暗色主题设计系统
- Radix UI — 基础组件
- Lucide Icons — 图标
- TypeScript strict

### 已完成页面
1. `/` — 首页（Hero + 五种创作模式 + 创意输入框）
2. `/login` — 登录页（邮箱密码 mock）
3. `/projects` — 项目列表（卡片 + 底部 Tab + 积分/等级）
4. `/projects/[id]/canvas` — 画布编辑器（三栏布局）
   - 左侧：AI 聊天面板（可折叠，mock 对话）
   - 中间：React Flow 画布（5 种模式各有不同 DAG 节点流）
   - 右侧：节点详情面板（8 种节点类型全覆盖）

### 节点详情面板
- CheckPoint: 语言/评级/比例/集数/时长/视觉风格/创意描述
- StoryBible: 故事方案多选一
- CharacterPack: 角色卡片 + 配音选择
- PlanningCenter: 概览/封面/剧集三 Tab
- Script: 分场剧本 + 对白 + VO
- SceneDesign: 场景视觉参考 + 生成状态
- SegmentDesign: 分镜列表 + 镜头参数 + 生成状态
- Compose: 导出设置 + 预览 + 剪映工程/MP4

### 构建状态
- `next build` 通过 ✅
- 零错误零警告

### 待完善
- 国际化 (next-intl) 尚未接入
- Mock API 层（目前直接用 store mock）
- 更多交互细节（节点拖拽连线、状态流转动画）
- 响应式适配（移动端）

---

## G 代码评审 + 重构（第二轮）

### 触发
G 完成第一轮代码评审 + Drama.Land 源码深度分析，栋少看了 demo 反馈差异太大，需要重构。

### P0 - 配色方案全局改造
- 紫色(#a855f7) → 红色(#C0031C)，accent: #FF4D4D
- 背景：#0a0a0f → 纯黑 #000000
- 文字透明度层级：white/80、white/60、white/40、white/20
- 边框：white/10，输入框：bg-white/5、bg-white/10
- 按钮：rgba(192,3,28,0.20)，hover rgba(192,3,28,0.25)
- globals.css CSS 变量一键全局生效

### P0 - 首页重构
- 呼吸动画背景：3层 radial-gradient + breathe 动画（替代视频背景）
- 红色渐变公告栏（可关闭）
- 固定透明导航栏（bg-black/30 backdrop-blur-md）
- Hero 标题：text-7xl~[110px]，skewX(-15deg) rotate(-5deg)，text-shadow 发光动画
- 毛玻璃搜索框：backdrop-blur-3xl border-white/10 bg-white/10
  - 内嵌底部工具栏：上传音频 + 素材 + 模式pill tabs(SINGLE/SEASON/SCRIPT/MV/小红书) + 语言 + 字数 + Generate
- Showcases 区域：3列作品网格 + "The DreamX Lands" 标题
- 完整 Footer：产品/资源/法律链接

### P1 - 画布逐步解锁
- 只渲染 completed + active + 首个 pending（带锁定提示"完成上一步后解锁"）
- 锁定节点 opacity-40，不可点击打开详情
- active 节点有 pulse-glow 动画

### P1 - 项目列表 Tab
- emoji(🏠📁🎨) → lucide 图标(Home/FolderOpen/Palette)
- active 状态用 #C0031C 红色

### P2 - 代码质量
- BaseWorkflowNode：8个节点组件统一抽取，消除重复代码
- DetailPanel：8个 detail 组件改为 next/dynamic 懒加载
- Logo 加 'use client'，颜色改为红色渐变
- img → next/image（projects page + characterpack detail）
- canvas-layout.ts：musicTypes/redbookTypes 不再用 checkpoint 占位

### 构建 & 推送
- build 零错误零警告 ✅
- GitHub: git@github.com:Mv-Victor/dreamX-.git (main branch)
- commit: b8d44a8
- 已通过 sessions_send 向 G 汇报

### G 的评审要点（备忘，后续可能还有）
- Drama.Land 配色是红黑白，不是紫色
- 首页搜索框模式选择器应内嵌，不是独立大按钮组
- Hero 标题要有斜体+发光动效
- 节点不应一次性全显示，要逐步解锁
- 重复代码要抽取公共组件

---

## 第三轮更新（API 层 + UI 细节）

### API 对接层设计
- `docs/api-design.md` - 完整 API 接口文档（仿 Drama.Land 规范）
  - 用户模块：get_user_info / credits/summary / points_config
  - 项目模块：list / create / detail / resume
  - 画布模块：main_canvas / visual_style_list / voices
  - AI 生成：story_bible / characters / script
  - 档案馆：showcase/items
  - 统一响应格式：`{ code, message, data }`
  - 错误码规范：200/400/401/403/404/429/500/503

### Mock API 实现
- `src/mock/api/types.ts` - API 类型定义
- `src/mock/api/user.ts` - 用户相关 API
- `src/mock/api/project.ts` - 项目 CRUD
- `src/mock/api/showcase.ts` - 档案馆作品列表
- `src/mock/api/query.ts` - 画布/风格/配音查询

### UI 修复
1. 首页搜索框模式切换 - 修复换行（nowrap + overflow-x-auto）
2. 项目列表底部 Tab - Drama.Land 风格（fixed + backdrop-blur-xl + 红色高亮）
3. 画布右侧详情面板 - CheckPointDetail 重新设计
   - Section 分组（图标 + 英文 label uppercase）
   - 更精细的表单样式（边框/间距/动效）
   - 视觉风格卡片选中状态
   - 滑块进度条样式

### 待完成
- 档案馆画廊瀑布流布局
- 其他节点详情面板对齐新风格
- 首页左侧 SVG 图标

---

## 第四轮更新（P0 任务完成）

### P0 任务 1: API 层基础架构
- `src/lib/api/client.ts` — 基础请求封装 + ErrorCode 枚举 + ApiClientError
- `src/lib/api/projects.ts` — 项目 API (list/get/create/resume)
- `src/lib/api/nodes.ts` — 画布节点 API (getMainCanvas/updateNodeStatus)
- `src/lib/api/characters.ts` — 角色 API (getVoices/generateCharacters)
- `src/types/api.ts` — 完整 API 类型定义
- `/root/.openclaw/workspace-g/docs/dreamx-api-spec.md` — API 规范文档

### P0 任务 2: 画布详情面板精细化改造
- checkpoint-detail.tsx — Section 分组 + 图标 + 精细表单
- characterpack-detail.tsx — 角色卡片 + 配音选择 + 双按钮
- script-detail.tsx — 分场剧本 + 对白 + VO
- scenedesign-detail.tsx — 场景视觉参考 + 生成状态遮罩
- segmentdesign-detail.tsx — 分镜列表 + 镜头参数 + 缩略图
- compose-detail.tsx — 预览区域 + 导出设置卡片

统一设计语言（对齐 Drama.Land）：
- 卡片：rounded-lg border border-white/10 bg-white/5 p-3
- Section：图标 + 英文 label uppercase
- 状态 badge：completed(green)/generating(red+spin)/pending(lock)
- 双按钮：左 outline 右 primary

### 当前进度
- ✅ P0 API 层设计 + 文档
- ✅ P0 画布详情面板精细化
- ✅ P1 档案馆画廊 (/showcases)
- 🔲 P1 底部 Tab Bar 确认（等 G 指示）
- 🔲 P1 其他节点详情面板（StoryBible/PlanningCenter 等）

---

## 第五轮更新（全部待完成任务完成）

### StoryBible 详情面板
- 故事方案多选一卡片（3 个方案）
- 类型标签 + logline + synopsis
- 选中状态高亮（红色边框 + 背景）
- 双按钮布局

### PlanningCenter 详情面板
- 概览/封面/剧集 三 Tab 切换
- 概览：核心叙事 + 类型标签
- 封面：AI 生成占位 + 重新生成/上传参考按钮
- 剧集：4 集列表（标题 + 摘要 + 场景数）
- 确认规划按钮

### 全部完成清单
✅ P0-1: API 层基础架构 + 文档
✅ P0-2: 画布详情面板精细化（8 个面板）
✅ P1-1: 档案馆画廊（瀑布流）
✅ P1-2: 底部 Tab Bar（Drama.Land 风格）
✅ P1-3: StoryBible 详情面板
✅ P1-4: PlanningCenter 详情面板

### 最终提交
- Commit: 155709e
- 所有详情面板统一设计语言
- 已通知栋少验收

---

## 第六轮更新（P1 响应式优化）

### 首页响应式优化
- 模式切换器：移动端隐藏 (`hidden md:flex`)
- 当前模式标签：仅桌面端显示
- 移动端更简洁，桌面端保持完整功能

### 最终提交
- Commit: 31f483c
- Build: 零错误零警告
- 已通知 G 验收

### 全部任务完成清单
✅ P0-1: API 层基础架构 + 文档
✅ P0-2: 画布 8 个详情面板精细化
✅ P1-1: 档案馆画廊（瀑布流）
✅ P1-2: 底部 Tab Bar（Drama.Land 风格）
✅ P1-3: StoryBible 详情面板
✅ P1-4: PlanningCenter 详情面板
✅ P1-5: 首页响应式优化

等待栋少验收反馈。

---

## 第七轮更新（P1 组件化重构）

### 新增通用组件
- ui/detail-section.tsx — Section 分组组件
- ui/status-badge.tsx — 状态 Badge
- ui/button.tsx — Button 组件
- ui/badge.tsx — Badge 组件

### 重构 8 个详情面板
全部改用组件库，消除重复代码

### 代码质量提升
- Section 组件：100% 使用
- Button 组件：> 80% 使用率
- Badge 组件：> 80% 使用率
- 代码量减少约 150 行

### 提交
- Commit: 11088f5
- Build: 零错误零警告
- 已通知 G 验收

---

## 第八轮更新（P0 画布修复）

### P0 修复 1 - 节点数据更新逻辑
- 使用 initialLoadRef 避免每次 projectType 变化重置节点状态
- 使用函数形式更新节点，保留用户进度
- 添加 handleNodeComplete 用于解锁下一个节点

### P0 修复 2 - 连接验证
- 添加 isValidConnection 防止错误连接
- 只允许从上到下顺序连接（targetIdx === sourceIdx + 1）
- 防止自连接

### 提交
- Commit: 7543b6e
- Build: 零错误零警告
- 已通知栋少验收

### 进度汇总
- P0 必须修复：✅ 完成
- P1 组件化重构：✅ 完成（第八轮）
- P1 画布优化：⏳ 待执行（自定义 Edge/视口持久化/节点拖拽）
