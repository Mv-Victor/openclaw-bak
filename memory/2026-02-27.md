# DreamX Studio 项目记忆

## 项目概况
- **项目名称**: DreamX Studio - AI 驱动的全链路短剧创作平台
- **对标产品**: Drama.Land (https://drama.land/)
- **技术栈**: Next.js 14 + React Flow + Zustand + Immer + Tailwind CSS
- **项目路径**: `/root/dreamx-studio/`
- **启动时间**: 2026-02-27
- **负责人**: 栋少（黄智栋）

## 多 Agent 协作流程
- **G（总指挥）**: 代码审核、Drama.Land 差异分析、派工、验收
- **啾啾（工程师）**: 代码实现、功能开发
- **协作模式**: G 审核 → 输出差异报告 → 派工 → 啾啾执行 → G 验收 → 循环迭代

## 核心设计决策（2026-02-27）

### 配色方案
- 主色：`#C0031C`（红色）
- 强调色：`#FF4D4D`
- 背景：纯黑 `#000000`
- 按钮背景：`rgba(192,3,28,0.20)`，hover `rgba(192,3,28,0.25)`
- 文字透明度：`white/80`、`white/60`、`white/40`、`white/20`
- 边框：`white/10`

### 首页设计
- Hero 标题：`text-7xl lg:text-8xl` + `skewX(-15deg) rotate(-5deg)` + 发光动画
- 搜索框：毛玻璃效果 `backdrop-blur-3xl bg-white/10 rounded-xl`
- 模式切换器：内嵌到底部工具栏，pill tab 样式，移动端隐藏（`hidden md:flex`）
- 呼吸背景：3 层 radial-gradient 叠加动画

### 画布详情面板
- 统一 Section 组件（图标 + label uppercase + children）
- 卡片样式：`rounded-lg border border-white/10 bg-white/5 p-3`
- 状态 badge：completed(green) / generating(red+spin) / pending(lock)
- 双按钮布局：左 outline(重新生成) / 右红色 primary(确认)
- 8 个面板全部精细化：Checkpoint / StoryBible / CharacterPack / PlanningCenter / Script / SceneDesign / SegmentDesign / Compose

### 底部 Tab Bar
- 保持底部设计（不改成左侧边栏）
- 样式：`fixed bottom-0 + bg-black/80 backdrop-blur-xl`
- 3 个 Tab：首页 / 存档 / 资产
- 选中态：红色背景 `rgba(192,3,28,0.20)` + 红色高亮 `#FF4D4D`

### API 层设计
- 基础响应格式：`{ code, message, data }`
- 错误码：0=成功，1001-1006=客户端错误，2001-2002=生成错误，5000=服务器错误
- MOCK_MODE 开关控制
- 文档：`/root/.openclaw/workspace-g/docs/dreamx-api-spec.md`（556 行）

## 档案馆画廊（/showcases）
- 瀑布流布局：CSS columns（`columns-1 sm:columns-2 lg:columns-3 xl:columns-4`）
- 筛选 Tab：全部 / 视频 / 图片
- Mock API：`src/mock/api/showcase.ts`

## 代码质量指标（2026-02-28 02:15）
- Build 错误：0
- Build 警告：0
- API 类型覆盖率：100%
- 详情面板改造：8/8
- 组件化重构：完成（DetailSection/StatusBadge/Button/Badge）
- DetailSection 使用率：100%
- Button 使用率：100%
- 代码量减少：71 行（207 insertions, 278 deletions）
- P0 修复：完成（节点数据更新 + 连接验证 + 节点完成回调）
- React Flow 评分：80/100 → 90/100（P0 修复后）
- Drama.Land 还原度：~90%

## Git 提交记录
- 7543b6e - P0 修复（节点数据更新 + 连接验证 + 节点完成回调）
- 11088f5 - P1 组件化重构完成
- 31f483c - 首页响应式优化
- 1d51bba - 画布详情面板精细化改造
- 9efa923 - API 层基础架构
- 849e29c - 档案馆画廊页面
- b8d44a8 - P0+P1+P2 全面重构

## 待办事项（P2）
- [ ] 画布节点图标细化
- [ ] 生成动画增强
- [ ] 真实 API 对接（MOCK_MODE → false）
- [ ] 图片懒加载、虚拟滚动
- [ ] 全局 ErrorBoundary

## 重要文档
- `/root/.openclaw/workspace-g/docs/dreamx-audit-v2.md` - 第二轮审核报告
- `/root/.openclaw/workspace-g/docs/dreamx-audit-v3.md` - 第三轮审核（组件化重构）
- `/root/.openclaw/workspace-g/docs/dreamx-api-spec.md` - API 规范文档
- `/root/.openclaw/workspace-g/docs/dreamx-review-v1.md` - 第一轮重构指令
- `/root/.openclaw/workspace-g/docs/dreamx-react-flow-review.md` - React Flow 专项评审
- `/root/.openclaw/workspace-g/docs/dreamx-p0-fix-review.md` - P0 修复验收报告

## P0 修复关键模式（2026-02-28）

### 节点数据更新（避免重置）
```typescript
const initialLoadRef = useRef(true);

// 首次加载
useEffect(() => {
  if (initialLoadRef.current) {
    setNodes(initialNodes);
    initialLoadRef.current = false;
  }
}, []);

// 后续更新（保留进度）
useEffect(() => {
  setNodes((prev) => prev.map((node) => {
    const newNode = initialNodes.find((n) => n.id === node.id);
    return newNode ? { ...node, data: { ...node.data, ...newNode.data } } : node;
  }));
}, [initialNodes]);
```

### 连接验证
```typescript
const isValidConnection = useCallback((connection) => {
  if (connection.source === connection.target) return false;
  const sourceIdx = parseInt(source.split('-')[1]);
  const targetIdx = parseInt(target.split('-')[1]);
  return targetIdx === sourceIdx + 1; // 只允许顺序连接
}, []);
```

### 节点完成回调（渐进式解锁）
```typescript
const handleNodeComplete = useCallback((nodeId: string) => {
  const currentNodes = getNodes();
  const idx = currentNodes.findIndex((n) => n.id === nodeId);
  if (idx >= 0 && idx < currentNodes.length - 1) {
    updateNodeData(currentNodes[idx + 1].id, { status: 'active', locked: false });
    updateNodeData(nodeId, { status: 'completed' });
  }
}, [getNodes, updateNodeData]);
```
