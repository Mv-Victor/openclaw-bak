- **2026-04-07**: 栋少反馈 `rss-daily-digest` 任务由于原先配置的 `qwencode/qwen3.5-plus` key 过期报错 (HTTP 401)，已将该定时任务的模型配置更新为 `claude-sonnet-4-6` 并触发重跑。
- **2026-04-07**: 在最小化部署单容器版本的 `new-api` (SQLite 版) 时，检查发现其 `setup` 状态仍为 false，且数据库 `one-api.db` 中的 `users` 表为空，这意味着初始化注册管理员账号的流程尚未完成。已告知栋少需提供具体的报错场景或截图进行排查。
